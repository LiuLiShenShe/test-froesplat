# 实验状态板

## 阶段一：工作区和规划

| 项目 | 状态 | 说明 |
| --- | --- | --- |
| 创建实验工作区 | 已完成 | 路径为 `计算机与电子农业特刊实验工作区/`。 |
| 创建索引文件 | 已完成 | 包含说明、任务计划、工作区索引和状态板。 |
| 创建结果模板 | 已完成 | 位于 `04-结果表格模板/`。 |
| 修改默认论文 | 未执行 | 按要求保持不变。 |

## 阶段二：RAP-FSAM3 算法模块

| 模块 | 状态 | 主要目的 | 阻塞信息 |
| --- | --- | --- | --- |
| 多提示词候选与旧 A1 自动选择 | 已实现，小批量通过 | 暴露并缓解单一提示词语义偏差 | `KongQueZhuYu` A5 10 帧生成 P1-P5 候选 50 张；`XianKeLai1` GT1 诊断显示旧 A1 存在选错 P3 的风险 |
| A1s 语义门控选择 | 已实现并验证 | 用植物目标框、花盆/桌面/邻近植物干扰框约束候选选择 | `XianKeLai1` GT1 已纠正旧 A1 选错 P3；`KongQueZhuYu` GT5 输出 `语义门控评分.csv` 和调试图，5/5 仍选择 P2 |
| A2 结构化正负提示 | 已实现，完整后端 GT5 已验证有效 | 抑制花盆、土壤、支撑物和背景误分 | `KongQueZhuYu` GT5 完整 `sam3_if_supported` 重跑中 F1/mIoU 提升、泄漏下降 |
| 旧 A3/A4/A5 保护性链路 | 已实现，小批量通过，作为日志/保底保留 | 残差保底、突变检测、几何 low/ok 诊断 | 当前标注子集未显示独立二值增益，不作为新版主创新消融项 |
| A5c 重建一致性修正 | 已实现并验证 | 把 COLMAP/2DGS 几何反馈转化为正向补充和局部 mask delta | `KongQueZhuYu` GT5 完整后端重跑 5/5 接受几何修正，平均 delta 约 0.000166；负向删除默认关闭 |
| 最大连通域主体筛选 | 已复查，作为保底有效 | 去除孤立小碎片和噪声连通域 | 原 A5 候选阶段已默认启用；专项离线对照显示 `KongQueZhuYu` 10 帧中 9 帧本来就是单连通域，`0007` 只移除 2 像素，说明当前泄漏主要是同一连通域粘连问题 |

## 阶段二路径定位结果

| 项目 | 状态 | 路径 |
| --- | --- | --- |
| SAM3 旧脚本 | 已定位，作为历史基线参考 | `/data/fj/F2DMAS/03-SAM/segment_v2.py` |
| SAM3 默认源码和权重 | 已验证，可用 | `/data/fj/F2DMAS/第三方源码/SAM3-latest` 当前提交 `8e451d5`，`/data/fj/F2DMAS/sam3/sam3.pt` |
| SAM3 历史源码 | 已定位但不再默认使用 | `/data/fj/F2DMAS/sam3-main` 含空字节损坏文件 |
| 旧 SAM 掩膜 | 已定位，可用 | `/data/fj/F2DMAS/03-SAM/<样本名>` |
| 统一 SAM 掩膜入口 | 已定位，可用 | `数据管理/03-分割Mask/02-sam_masks/<样本名>` |
| RAP-FSAM3 新输出 | 已创建，已有 A1/A5 冒烟和小批量输出 | `数据管理/03-分割Mask/05-RAP-FSAM3掩膜/` |
| RAP-FSAM3 运行记录目录 | 已创建，脚本和命令示例已写入 | `数据管理/07-运行脚本与超参/S20-RAP-FSAM3掩膜生成与验证/` |
| 人工标注 | 已定位，待确认 GT 转换方式 | `数据管理/03-分割Mask/01-gt_masks/` |
| COLMAP 位姿 | 已定位，可用 | `数据管理/02-位姿COLMAP/03-final_locked/` |
| 2DGS 入口 | 已定位，可用 | `/data/fj/F2DMAS/2d-gaussian-splatting-main` |

## 阶段三：优先实验

| 实验 | 状态 | 优先级 | 说明 |
| --- | --- | --- | --- |
| 实验二：提示词鲁棒性 | GT5 表格和图板已完成，可选扩展 | 最高 | `KongQueZhuYu` GT5 已填 P1-P5 与多提示词选择表；P2 稳定、P4 基本失效，已能引出 RAP-FSAM3 多提示词机制。 |
| 实验三：RAP-FSAM3-v2 模块消融 | 分割主表已完成，待新版图板和下游2DGS衔接 | 最高 | 新版 A0/A1/A1s/A2/A5c 主表已填；A2 是主分割增益，A5c 是几何一致性补充；还需正式 A1/A1s 与 A2/A5c 图板。 |
| 实验四：视觉基础模型先验注入 2DGS 位置消融 | 下一步主任务，已有可复用结果待整理/补跑 | 最高 | S10/S12/S18 已有 A0-A6 和 A6/full 跨样本证据；需按 B0-B5 口径填写特刊表，必要时补跑缺项。 |

## 阶段四：扩展实验

| 实验 | 状态 | 优先级 | 说明 |
| --- | --- | --- | --- |
| 实验一：视觉基础模型横向对比 | S23 9 方法分割表、监督参照和图件资产已闭环；下游小闭环待补 | 中高 | 已改为外部 VFM/open-vocabulary 模型 + 检测分割 + 少量监督参照口径；`S23_Experiment1_VFM_Benchmark` 已完成 Florence-2、CLIPSeg、SAM2 oracle、SAM3 单提示词、Grounded-SAM、Grounded-SAM2、RAP-FSAM3-v2、U-Net、DeepLabv3+ lite 在 `KongQueZhuYu` GT5 + `XianKeLai1` GT1 的统一分割评估。分割表已补全主表字段、备注、失败原因，并把已完成方法映射到 `figure_asset_index.csv`。X-Decoder/OpenSeeD 本轮暂停；SegFormer 可选扩展；下游优先 RAP-FSAM3-v2、SAM3 单提示词、Grounded-SAM 或 CLIPSeg 三方法小闭环。 |
| 实验五：掩膜到表型误差敏感性 | 当前表型汇总、图件资产和 source_data 已闭环；严格多掩膜敏感性待补 | 中高 | 已用 `植株数据.xlsx` 生成株高、冠幅、叶长、叶宽 MAE/RMSE/MAPE/Bias/R2；叶宽 MAPE 最高、R2 最低。图件位于 `05-图件与论文映射/实验五_掩膜到表型误差敏感性/`，并用 `strict_multimask_status.csv` 标注严格多掩膜重建边界。SEEM/SAM3/RAP-FSAM3 等多掩膜来源统一重建和表型测量仍待补。 |

## 状态说明

- `已规划`：已有方案，但尚未实现或运行。
- `当前可复现表已生成`：已有本地证据已经入表，但完整补跑仍待完成。
- `运行中`：实验正在执行。
- `部分完成`：已有部分结果，但表格未完整。
- `已完成`：指标、表格和验证说明均完成。
- `受阻`：缺少数据、代码、模型输出或标注。
