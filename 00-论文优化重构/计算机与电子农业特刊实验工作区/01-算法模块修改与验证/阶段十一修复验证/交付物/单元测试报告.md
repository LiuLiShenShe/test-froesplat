# 阶段十一 单元测试报告

日期：2026-08-29
测试文件：`tests/test_phase11.py`
运行：`python3 -m pytest tests/test_phase11.py -v` → **9 passed**

## 测试覆盖（§9 六类）

| 测试类 | 验证点 | 结果 |
| --- | --- | --- |
| TestGtSplit | GT 几何+颜色启发式拆分 plant/pot/cube；cube 不进 plant GT | PASS |
| TestCandidateGen | per_instance 模式产生独立候选（不 OR 合并，instance_id 独立） | PASS |
| TestEmptyMaskScore | 空掩膜候选 `total_score==0` 且 `empty_flag=True`，时序项不默认满分 | PASS |
| TestP2P6Collapse | P2==P6（面积/盆区一致）被 `detect_semantic_collapse` 判为坍缩；差异明显则否 | PASS |
| TestA6Coords | A6 跨视角共识默认关（`use_cross_view_consensus`=False）；配准开关默认关 | PASS |
| TestA7SingleId | `select_single_object_id` 只返回单个匹配 ID，不 OR 所有 object ID | PASS |

## 结论
所有核心修复逻辑（GT 拆分、per_instance 候选、空掩膜门控、P2/P6 坍缩检测、A6 默认关、
A7 单对象 ID）均有对应单元测试覆盖且通过，无样本名/文件名硬编码特判。
