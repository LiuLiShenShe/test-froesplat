# S12 代表样本 A6 扩展说明

## 目标

把 `A6_foreground_track_init_fg_rgb_alpha_bg` 从 `KongQueZhuYu` 单样本扩展到 3 个结构互补样本，验证 foreground-object reconstruction 主线不是单样本偶然。

## 样本

| 样本 | 角色 | mask pattern | 状态 |
|---|---|---|---|
| `KongQueZhuYu` | 复杂背景/主样本 | `mask_{stem}.png` | A0-A6 已完成 |
| `XianKeLai1` | 薄叶/细结构 | `{stem}.png` | 本轮新增 A6；sanitized scene 保留 203/208 张，剔除 4 张不可读图和 1 张不可读 mask |
| `CaoMei2` | 密集叶/遮挡 | `mask_{stem}.png` | 本轮新增 A6；sanitized scene 保留 203/210 张，剔除 7 张不可读 mask |

`CaoMei1` 本轮暂缓：它有 GT 价值，但当前 SAM mask 对 final COLMAP 图像覆盖为 172/210，需要先补齐或重对齐 mask。
`DouBanLv3` 本轮暂缓：原本可作为密集叶/遮挡候选，sanitized scene 可保留 198/200 张，但 COLMAP camera model 为 `OPENCV`，当前 2DGS loader 只支持 undistorted `PINHOLE` / `SIMPLE_PINHOLE`，因此不作为本轮主结果。

## 运行顺序

```bash
/data/fj/F2DMAS/2d-gaussian-splatting-main/venv/bin/python \
  00-论文优化重构/数据管理/07-运行脚本与超参/S4-2DGS-baseline回归/scripts/run_2dgs_baseline.py \
  --config 00-论文优化重构/数据管理/07-运行脚本与超参/S12-代表样本A6扩展/configs/xiankelai1_A6_foreground_track_init_fg_rgb_alpha_bg.json

/data/fj/F2DMAS/2d-gaussian-splatting-main/venv/bin/python \
  00-论文优化重构/数据管理/07-运行脚本与超参/S4-2DGS-baseline回归/scripts/run_2dgs_baseline.py \
  --config 00-论文优化重构/数据管理/07-运行脚本与超参/S12-代表样本A6扩展/configs/caomei2_A6_foreground_track_init_fg_rgb_alpha_bg.json
```

训练完成后，对每个输出目录运行 foreground-object eval，写入 `foreground_object_results.json`。

sanitized scene 存放在：

```text
00-论文优化重构/数据管理/02-位姿COLMAP/04-sanitized_for_A6/<sample>
```

它只用于本轮 A6 代表样本扩展，不覆盖 `03-final_locked` 原始锁定结果。

## 后续优先级

1. M4 on A6：先形成稳定收益，并服务 plant-only export / mesh。
2. M1 on A6：小规模对比输入视角质量/覆盖。
3. M5：作为 phenotype-ready mesh 的后半部分补强。
