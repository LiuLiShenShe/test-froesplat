## 一、 对比实验清单 (Comparative Studies)

必须在三个核心维度上建立针对竞品的碾压或原理解释，证明系统的不可替代性。

### 1. 语义解耦基线对比 (证明 SAM3 的前置必要性)

**实验目的**：证明在全自动（无人工提示）条件下，SAM3 对复杂植物高频拓扑的分割鲁棒性。
**数据集截取**：挑选 3-5 张极端工况图像（包含细长茎秆、高频叶缘锯齿、与植物颜色相近的环境背景），人工精细标注 Ground Truth (GT) 掩码。
**执行动作**：运行以下三种**全自动**基线，并提取定性与定量数据。

| 对比基线 | 算法属性 | 核心攻击点 / 预期失效表现 | 需要提取的量化指标 |
| --- | --- | --- | --- |
| **ExG + Otsu** | 传统农业超绿特征阈值 | 在高反光区域出现空洞，红褐色非绿茎秆彻底断裂。 | mIoU, Boundary F1 Score |
| **YOLOv8-Seg** | 预训练实例分割 (不微调) | 边界呈现严重的块状平滑，丢失细长叶柄结构。 | mIoU, Boundary F1 Score |
| **SAM 1 (AMG 模式)** | 大模型初代全自动生成 | 针对复杂光影叶片产生过度分割 (碎裂为多块掩码)。 | mIoU, Boundary F1 Score |
| **SAM3 (本系统)** | 强语义先验解耦 | **维持拓扑连续性，精确保留高频边缘。** | **最高 mIoU / Boundary F1** |

### 2. 光学渲染保真度对比 (证明辐射场视觉优势)

**实验目的**：确立 SuGaR 在视觉模态上的可用性，并坦承其因几何约束对极致锐度的微小让步。
**数据集截取**：留出 10% 视角的图像作为未参与训练的测试集。
**执行动作**：基于收敛后的高斯参数，渲染测试集视角的 2D 图像并计算误差。

| 对比基线 | 核心表征机制 | 核心攻击点 / 预期表现 | 需要提取的量化指标 |
| --- | --- | --- | --- |
| **Instant-NGP** | 纯隐式辐射场 | 边界密度发散，呈现严重的放射状伪影。 | PSNR, SSIM, LPIPS |
| **3DGS** | 纯显式三维椭圆球 | 弱纹理区域呈现云雾状重影 (Floaters)。 | PSNR, SSIM, LPIPS |
| **2DGS** | 纯显式二维椭圆盘 | 极限光学锐度极高，作为上限参考标尺。 | 提取极高 SSIM 均值 |
| **SuGaR (本系统)** | SDF 隐式正则化高斯 | **渲染极其纯净，SSIM 略低于 2DGS，但不影响农艺视觉阈值。** | **PSNR, SSIM, LPIPS** |

### 3. 网格拓扑连续性对比 (核心物理机制辩护)

**实验目的**：证明零厚度非刚性曲面的正反面法线干涉导致显式二维建模网格化彻底失效。
**执行动作**：针对同一极薄叶片（如白掌单叶），分别从 2DGS 和 SuGaR 导出 `.ply` 或 `.obj` 网格文件。导入 3D 软件，强制开启 **Solid + Wireframe (实体+线框)** 模式。
**数据留存要求**：

* 截取 2DGS Mesh 视口：标记贯穿性孔洞、双层皮撕裂、法线翻转造成的黑色死区。
* 截取 SuGaR Mesh 视口：展示均匀连续的三角面片拓扑。

---

## 二、 物理表型量化验证清单 (Geometric Phenotyping)

**实验目的**：利用交互式提取建立金标准，证明数字孪生在绝对空间尺度上的几何逼真度。
**执行动作**：在 CloudCompare 中利用物理标定物完成全局尺度恢复。针对 21 株样本，提取虚拟测量值并与手工真值对齐。

| 验证模块 | 测量项目与样本量 | 需要输出的图表类型与统计参数 |
| --- | --- | --- |
| **二维形态** | 叶长 ($N=63$) + 叶宽 ($N=63$) | 合并为散点图 ($N=126$)。计算 $R^2$ (需 $>0.95$), RMSE, 绘制 1:1 基准线与 95% 置信区间。 |
| **三维宏观** | 株高 ($N=21$) + 冠幅 ($N=21$) | 合并为散点图 ($N=42$)。计算 $R^2$, RMSE, 绘制拟合线。 |
| **空间姿态** | 绝对叶倾角 ($N=21$) | **Bland-Altman 图**。计算差值均值，绘制 $\pm 1.96 SD$ 一致性界限。 |
| **极限分辨率** | 茎粗 ($N=21$) + 叶柄长 ($N=6$) | 数据表格。计算 MAE (平均绝对误差)，证明达到毫米级量化下限。 |

---

## 三、 消融实验清单 (Ablation Studies)

**实验目的**：通过控制变量法剥离特定模块，量化其对系统级数字孪生质量的不可或缺性。
**执行动作**：选择 1-2 盆具备复杂环境的样本，执行以下截断训练，并提取对比数据。

| 实验组别 | 剥离机制 | 需要获取的量化/定性结果 | 结论指向 |
| --- | --- | --- | --- |
| **w/o SDF Regularization** | 剔除 SuGaR 正则化项 (退化为普通高斯) | 1. 计算极薄叶片的面片孔洞率。2. 对比表面积积分计算结果的负向系统误差。 | 证明隐式正则化是获取可靠积分特征的唯一途径。 |
| **w/o SAM3 Semantic Prior** | 不使用掩码，输入全图直接训练 | 1. 记录显存峰值 (Peak VRAM) 溢出量与训练耗时增量。2. 截取植物基底与花盆/桌面发生不可逆拓扑粘连的 3D 视角。 | 证明语义解耦对算力聚焦与底部拓扑剥离的必要性。 |
| **w/o FFT Filter** | 保留视频序列中的动态模糊帧 | 1. 记录 COLMAP 相机位姿解算的漂移量。2. 截取最终渲染图中由于位姿错误导致的高频重影。 | 证明输入源物理清洗对高保真重建的前置保护作用。 |

---

**阶段任务执行检查点**：
所有 `mIoU`, `PSNR/SSIM`, `RMSE` 数据必须精确到小数点后三位。所有包含对比性质的三维模型截图，必须保证相机视角（Camera Viewpoint）绝对一致。